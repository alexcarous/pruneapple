disabled_rules:
  - trailing_whitespace
  - line_length
  - type_body_length

opt_in_rules:
  - empty_count
  - force_unwrapping

included:
  - Targets/Pruneapple/Sources

excluded:
  - .build
  - DerivedData
  - Targets/Pruneapple/Tests
